---
const socialLinks = [
  { href: 'https://linkedin.com/in/megha-s-93826b152/', icon: 'linkedin', label: 'LinkedIn' },
  { href: 'https://github.com/meghasivarajan/', icon: 'github', label: 'GitHub' },
  { href: 'mailto:meghasivarajan111@gmail.com', icon: 'email', label: 'Email' },
];
---

<section id="hero" class="hero">
  <!-- Background Effects -->
  <canvas id="particle-canvas" class="particle-canvas"></canvas>
  <div class="grid-pattern"></div>
  <div class="floating-shape shape-cyan" style="width: 400px; height: 400px; top: 10%; left: -10%;"></div>
  <div class="floating-shape shape-purple" style="width: 300px; height: 300px; bottom: 10%; right: -5%;"></div>

  <style>
    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow: hidden;
      padding-top: 80px;
    }

    .particle-canvas {
      position: absolute;
      inset: 0;
      z-index: 0;
    }

    .hero-content {
      position: relative;
      z-index: 1;
      text-align: center;
      padding: var(--space-3xl) 0;
    }

    /* Greeting */
    .hero-greeting {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: var(--space-sm);
      margin-bottom: var(--space-md);
    }

    .greeting-wave {
      font-size: 2rem;
      animation: wave 2.5s ease-in-out infinite;
      transform-origin: 70% 70%;
    }

    @keyframes wave {
      0%, 100% { transform: rotate(0deg); }
      10%, 30%, 50% { transform: rotate(14deg); }
      20%, 40% { transform: rotate(-8deg); }
      60% { transform: rotate(0deg); }
    }

    .greeting-text {
      font-family: var(--font-mono);
      font-size: 1.25rem;
      color: var(--accent-cyan);
    }

    /* Name */
    .hero-name {
      font-size: clamp(3rem, 12vw, 8rem);
      font-weight: 700;
      line-height: 1;
      margin-bottom: var(--space-md);
    }

    .name-text {
      color: var(--text-primary);
    }

    .name-highlight {
      background: var(--gradient-primary);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    /* Role */
    .hero-role {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: var(--space-sm);
      font-family: var(--font-mono);
      font-size: 1.5rem;
      margin-bottom: var(--space-xl);
    }

    .role-bracket {
      color: var(--accent-cyan);
      opacity: 0.6;
    }

    .role-text {
      color: var(--text-primary);
    }

    /* Taglines */
    .hero-taglines {
      margin-bottom: var(--space-2xl);
    }

    .tagline-primary {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: var(--space-md);
      font-size: 1.25rem;
      color: var(--text-primary);
      margin-bottom: var(--space-sm);
      flex-wrap: wrap;
    }

    .vibe-badge {
      display: inline-flex;
      align-items: center;
      gap: var(--space-xs);
      padding: var(--space-xs) var(--space-md);
      background: linear-gradient(135deg, rgba(245, 158, 11, 0.2), rgba(239, 68, 68, 0.2));
      border: 1px solid rgba(245, 158, 11, 0.4);
      border-radius: var(--radius-full);
      font-size: 0.9rem;
      font-weight: 600;
      color: #f59e0b;
      animation: pulse-glow 2s ease-in-out infinite;
    }

    .tagline-secondary {
      font-size: 1.1rem;
      color: var(--text-secondary);
    }

    /* Terminal */
    .hero-terminal {
      max-width: 500px;
      margin: 0 auto var(--space-2xl);
      text-align: left;
      background: var(--bg-secondary);
      border: 1px solid var(--glass-border);
      border-radius: var(--radius-lg);
      overflow: hidden;
    }

    .terminal-header {
      display: flex;
      align-items: center;
      gap: var(--space-sm);
      padding: var(--space-sm) var(--space-md);
      background: var(--bg-tertiary);
      border-bottom: 1px solid var(--glass-border);
    }

    .terminal-dot {
      width: 12px;
      height: 12px;
      border-radius: 50%;
    }

    .terminal-dot.red { background: #ff5f56; }
    .terminal-dot.yellow { background: #ffbd2e; }
    .terminal-dot.green { background: #27ca40; }

    .terminal-title {
      margin-left: auto;
      font-family: var(--font-mono);
      font-size: 0.75rem;
      color: var(--text-muted);
    }

    .terminal-body {
      padding: var(--space-lg);
      font-family: var(--font-mono);
      font-size: 0.85rem;
      line-height: 1.8;
    }

    .code-keyword { color: var(--accent-purple); }
    .code-var { color: var(--accent-cyan); }
    .code-prop { color: #a5d6ff; }
    .code-string { color: #a5d6a7; }
    .code-bool { color: #f59e0b; }
    .code-comment { color: var(--text-muted); font-style: italic; }

    .terminal-cursor {
      display: inline-block;
      color: var(--accent-cyan);
      animation: blink 1s step-end infinite;
    }

    /* CTA */
    .hero-cta {
      display: flex;
      gap: var(--space-md);
      justify-content: center;
      flex-wrap: wrap;
      margin-bottom: var(--space-2xl);
    }

    /* Socials */
    .hero-socials {
      display: flex;
      gap: var(--space-lg);
      justify-content: center;
      margin-bottom: var(--space-3xl);
    }

    .social-link {
      width: 48px;
      height: 48px;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: var(--radius-full);
      background: var(--glass-bg);
      border: 1px solid var(--glass-border);
      color: var(--text-secondary);
      transition: var(--transition-base);
    }

    .social-link:hover {
      border-color: var(--accent-cyan);
      color: var(--accent-cyan);
      transform: translateY(-5px);
      box-shadow: var(--shadow-glow-cyan);
    }

    /* Scroll Indicator */
    .scroll-indicator {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: var(--space-sm);
      color: var(--text-muted);
      font-size: 0.875rem;
    }

    .scroll-mouse {
      width: 24px;
      height: 40px;
      border: 2px solid var(--text-muted);
      border-radius: 12px;
      display: flex;
      justify-content: center;
      padding-top: 8px;
    }

    .scroll-wheel {
      width: 3px;
      height: 8px;
      background: var(--accent-cyan);
      border-radius: var(--radius-full);
      animation: scrollWheel 2s ease-in-out infinite;
    }

    @keyframes scrollWheel {
      0%, 100% { transform: translateY(0); opacity: 1; }
      50% { transform: translateY(8px); opacity: 0.3; }
    }

    /* Responsive */
    @media (max-width: 768px) {
      .hero-terminal {
        display: none;
      }

      .hero-role {
        font-size: 1.1rem;
      }

      .tagline-primary {
        flex-direction: column;
        gap: var(--space-sm);
      }
    }
  </style>

  <div class="container hero-content">
    <!-- Greeting -->
    <div class="hero-greeting reveal">
      <span class="greeting-wave">👋</span>
      <span class="greeting-text">Hello, I'm</span>
    </div>

    <!-- Name -->
    <h1 class="hero-name reveal delay-100">
      <span class="name-text">Megha</span>
      <span class="name-highlight">S</span>
    </h1>

    <!-- Role -->
    <div class="hero-role reveal delay-200">
      <span class="role-bracket">&lt;</span>
      <span class="role-text">Senior Angular Developer</span>
      <span class="role-bracket">/&gt;</span>
    </div>

    <!-- Taglines -->
    <div class="hero-taglines reveal delay-300">
      <p class="tagline-primary">
        <span class="vibe-badge">⚡ Frontend Specialist</span>
        Building Scalable & High-Performance Web Apps
      </p>
      <p class="tagline-secondary">
        4+ years crafting enterprise-grade web applications with Angular & TypeScript
      </p>
    </div>

    <!-- Terminal Code Snippet -->
    <div class="hero-terminal reveal delay-400">
      <div class="terminal-header">
        <span class="terminal-dot red"></span>
        <span class="terminal-dot yellow"></span>
        <span class="terminal-dot green"></span>
        <span class="terminal-title">developer.ts</span>
      </div>
      <div class="terminal-body">
        <code>
          <span class="code-keyword">const</span> <span class="code-var">developer</span> = &#123;
          <br>&nbsp;&nbsp;<span class="code-prop">name</span>: <span class="code-string">"Megha S"</span>,
          <br>&nbsp;&nbsp;<span class="code-prop">expertise</span>: [<span class="code-string">"Angular"</span>, <span class="code-string">"TypeScript"</span>, <span class="code-string">"Flutter"</span>],
          <br>&nbsp;&nbsp;<span class="code-prop">passion</span>: <span class="code-string">"Building amazing experiences"</span>,
          <br>&nbsp;&nbsp;<span class="code-prop">vibeMode</span>: <span class="code-bool">true</span> <span class="code-comment">// Ship fast, iterate faster</span>
          <br>&#125;;
        </code>
        <span class="terminal-cursor">|</span>
      </div>
    </div>

    <!-- CTA Buttons -->
    <div class="hero-cta reveal delay-500">
      <!-- Buttons kept -->
      <a href="#projects" class="btn btn-primary">View Projects</a>
      <a href="/resume.pdf" class="btn btn-secondary" download="Megha_Resume.pdf">
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
          <polyline points="7 10 12 15 17 10"></polyline>
          <line x1="12" y1="15" x2="12" y2="3"></line>
        </svg>
        Download Resume
      </a>
    </div>

    <style>
      .btn-secondary {
        display: inline-flex;
        align-items: center;
        gap: var(--space-sm);
        padding: var(--space-sm) var(--space-xl);
        background: transparent;
        border: 1px solid var(--accent-cyan);
        border-radius: var(--radius-full);
        color: var(--accent-cyan);
        font-weight: 500;
        text-decoration: none;
        transition: var(--transition-base);
      }

      .btn-secondary:hover {
        background: rgba(0, 255, 255, 0.1);
        transform: translateY(-2px);
        box-shadow: 0 0 20px rgba(0, 255, 255, 0.2);
      }
    </style>

    <!-- Social Links -->
    <div class="hero-socials reveal delay-600">
      {socialLinks.map(link => (
        <a href={link.href} class="social-link" aria-label={link.label} target="_blank" rel="noopener noreferrer">
          {link.icon === 'linkedin' && (
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
              <path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/>
            </svg>
          )}
          {link.icon === 'github' && (
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
            </svg>
          )}
          {link.icon === 'email' && (
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
              <polyline points="22,6 12,13 2,6"></polyline>
            </svg>
          )}
          {link.icon === 'playstore' && (
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
              <path d="M3,20.5V3.5C3,2.91 3.34,2.39 3.84,2.15L13.69,12L3.84,21.85C3.34,21.6 3,21.09 3,20.5M16.81,15.12L6.05,21.34L14.54,12.85L16.81,15.12M20.16,10.81C20.5,11.08 20.75,11.5 20.75,12C20.75,12.5 20.53,12.9 20.18,13.18L17.89,14.5L15.39,12L17.89,9.5L20.16,10.81M6.05,2.66L16.81,8.88L14.54,11.15L6.05,2.66Z"/>
            </svg>
          )}
        </a>
      ))}
    </div>

    <!-- Scroll Indicator -->
    <div class="scroll-indicator reveal delay-700">
      <div class="scroll-mouse">
        <div class="scroll-wheel"></div>
      </div>
      <span>Scroll to explore</span>
    </div>
  </div>
</section>

<script lang="ts">
  // Particle Canvas Animation
  const canvas = document.getElementById('particle-canvas') as HTMLCanvasElement;
  if (canvas) {
    const ctx = canvas.getContext('2d');
    
    const resizeCanvas = () => {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    };
    resizeCanvas();
    window.addEventListener('resize', resizeCanvas);

    const particles: Array<{
      x: number;
      y: number;
      vx: number;
      vy: number;
      size: number;
      opacity: number;
      char: string;
    }> = [];

    const chars = ['<', '>', '/', '{', '}', '(', ')', ';', '=', '+'];
    
    const createParticle = () => {
      return {
        x: Math.random() * canvas.width,
        y: Math.random() * canvas.height,
        vx: (Math.random() - 0.5) * 0.5,
        vy: (Math.random() - 0.5) * 0.5,
        size: Math.random() * 14 + 10,
        opacity: Math.random() * 0.3 + 0.1,
        char: chars[Math.floor(Math.random() * chars.length)]
      };
    };

    for (let i = 0; i < 40; i++) {
      particles.push(createParticle());
    }

    const animate = () => {
      if (!ctx) return;
      ctx.clearRect(0, 0, canvas.width, canvas.height);

      particles.forEach(p => {
        p.x += p.vx;
        p.y += p.vy;

        if (p.x < 0 || p.x > canvas.width) p.vx *= -1;
        if (p.y < 0 || p.y > canvas.height) p.vy *= -1;

        ctx.font = `${p.size}px JetBrains Mono, monospace`;
        ctx.fillStyle = `rgba(0, 255, 255, ${p.opacity})`;
        ctx.fillText(p.char, p.x, p.y);
      });

      requestAnimationFrame(animate);
    };

    animate();
  }
</script>
